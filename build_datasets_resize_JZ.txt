tmux new -s convert
salloc --account=pab@cpu --nodes=1 --ntasks=1 --cpus-per-task=64 --time=06:00:00

module load ffmpeg
module load anaconda-py3/2024.06
source "$(conda info --base)/etc/profile.d/conda.sh"
conda activate physics-eval
cd $WORK/physics-eval/

HEIGHT=512 WORKERS=16 FFMPEG_THREADS=4 bash build_datasets_resize.sh

# # # # # # rm -R cache/datasets_512p/