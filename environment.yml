name: physics-eval

channels:
  - conda-forge
  - pytorch
  - nvidia
  - defaults

dependencies:
  - python=3.10
  - pip
  - numpy
  - scipy
  - pandas
  - matplotlib
  - datasets
  - pillow
  - "setuptools<81"
  - tqdm
  - pip:
      - -r environment_requirements.txt